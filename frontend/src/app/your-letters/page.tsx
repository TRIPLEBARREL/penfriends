"use client";

export default function YourLettersPage() {
  return (
    <div
      className="min-h-screen flex items-center justify-center"
      style={{
        fontFamily: "var(--font-bio-rhyme)", // Matches the Penfriends font
      }}
    >
      <h1 className="text-4xl font-bold">Your Letters</h1>
    </div>
  );
}
